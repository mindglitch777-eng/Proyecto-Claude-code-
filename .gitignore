__pycache__/
*.pyc
modelos_voz/
factory/oportunidades.db

# Base del motor de contenido: estado local, no se versiona.
motor/contenido.db

# Las pruebas de render las commitea el workflow probar-render, no la sesion.
previews/

# Archivos temporales del ciclo de mejora controlado (fabrica/laboratorio/):
# el arbol de cada intento se escribe ahi para pasarlo a los scripts de QA.
fabrica/laboratorio/.tmp_*.json

# Props de render del lote de 42 carruseles y su resumen de publicacion:
# los genera y commitea el workflow render-lote42-carruseles, no la sesion local.
remotion-spike/props_carrusel/
fabrica/carrusel/lote_42_resumen.json

# Entregas pendientes de aplicar al log (state/uploads.json/carruseles.json)
# -- un archivo por id, vive solo en el runner de cada corrida de
# subir-video.yml/subir-carrusel.yml/entregar-diario.yml hasta que el
# commit se aplica con exito (ver fabrica/subida/aplicar_pendiente.ts).
state/pendientes/
